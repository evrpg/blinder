{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash blinder/init.sh 2>&1 | tail -8",
            "description": "Fast verification after edits: structural checks + feature_list.json validity + at-most-one in_progress + compile/typecheck. Full test suite runs only at the review gate (blinder/init.sh --full)."
          }
        ]
      }
    ]
  }
}
